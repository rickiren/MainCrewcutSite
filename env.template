# Anthropic Claude API Configuration (for AI Chat)
# Get your API key from: https://console.anthropic.com/
VITE_CLAUDE_API_KEY=your_anthropic_api_key_here

# Resend Email Service Configuration
# For local development, use VITE_RESEND_API_KEY
# For production (Vercel), set RESEND_API_KEY in Vercel dashboard (without VITE_ prefix)
VITE_RESEND_API_KEY=your_resend_api_key

# Optional: Custom domain for sending emails (if you have one configured in Resend)
# IMPORTANT: Without a verified domain, you can ONLY send to your own email address (rickibodner@gmail.com)
# To send to other recipients:
#   1. Verify your domain at https://resend.com/domains (e.g., mail.crewcut.agency)
#   2. Set VITE_RESEND_FROM_EMAIL to use your verified domain email
#   3. Example: VITE_RESEND_FROM_EMAIL="CREW CUT <hello@mail.crewcut.agency>"
# VITE_RESEND_FROM_EMAIL=CREW CUT <hello@mail.crewcut.agency>

# Firebase Functions Configuration (optional - for production)
# Update the region if your Firebase project uses a different region (check Firebase Console after deployment)
VITE_FIREBASE_FUNCTION_URL=https://us-central1-crewcut-main.cloudfunctions.net/claude
