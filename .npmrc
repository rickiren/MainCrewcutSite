# Ensure optional dependencies are installed
# This helps with rollup's native dependencies in CI environments
# Note: Make sure your CI uses: npm install --include=optional
# Or for npm ci: npm ci --include=optional

